{
  "name": "prepare-for-accessbility-elements",
  "version": "1.0.0",
  "scripts": {
    "sass:build": "sass --no-source-map checkbox/style.scss:checkbox/style.css",
    "sass:watch": "sass --no-source-map --watch checkbox/style.scss:checkbox/style.css"
  },
  "devDependencies": {
    "sass": "^1.93.2"
  }
}